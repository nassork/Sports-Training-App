@model IEnumerable<AthleticTrainingWebApp.Models.Lesson>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Instructor
        </th>
        <th>
            Location
        </th>
        <th>
            Student Signed
        </th>
        <th>
            Lesson Fee
        </th>
        <th>
            Lesson Length
        </th>
        <th>
            Sport
        </th>
        <th>
            Date
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor.UserFullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School.SchoolName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.UserFullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LessonFee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LessonLength)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LessonSport)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LessonDate)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LId })
        </td>*@

        <td>

            @Html.ActionLink("Register for lesson", "RegisterStudent", "Lessons", new { id = item.LId }, null)

            

        </td>

    </tr>
}

</table>
